<%= render 'layouts/banner' %>

<div class="container">
  <div class="header">
    <h1>Lists</h1>
  </div>

  <%= render 'layouts/card' %>

  <div class="link">
    <p><%= link_to 'Create a new list', new_list_path(@list) %></p>
  </div>
</div>
