<div class="container">
  <%= render './components/movie_list' %>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.submit 'Bookmark', class: 'btn btn-primary' %>
  <% end %>
  <%= link_to 'Back', lists_path %>
</div>
