<div class="container">
  <h2>New list</h2>
  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit %>
  <% end %>

  <%= link_to 'Back to all lists', lists_path %>
</div>
